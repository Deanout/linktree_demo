<%= turbo_frame_tag "preview" do %>
  <iframe src="<%= profile_url(current_user) %>" 
        class="phone-border"
        width="100%" 
        height="100%" 
        frameborder="0" 
        scrolling="yes"></iframe>
<% end %>
